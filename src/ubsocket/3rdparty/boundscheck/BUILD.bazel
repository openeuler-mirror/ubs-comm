# third_party/libboundscheck.BUILD
cc_library(
    name = "boundscheck",
    srcs = glob([
        "src/**/*.c",
        "src/**/*.cpp",
    ], allow_empty = True),
    hdrs = glob([
        "include/**/*.h",
        "src/**/*.h",
        "**/*.inl",
    ], allow_empty = True),
    includes = ["include"],
    linkstatic = True,
    alwayslink = True,
    visibility = ["//visibility:public"],
)

cc_library(
    name = "boundscheck_headers",
    hdrs = glob([
        "include/**/*.h",
    ], allow_empty = True),
    includes = ["include"],
    visibility = ["//visibility:public"],
)
